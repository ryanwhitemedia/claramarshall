import React from "react"

import "./header.scss"

import Logo from "../../svgs/logo.svg"

const Header = () => (
  <header className="Header">
    <div>
      <Logo className="logo" />
    </div>
  </header>
)

export default Header
